server {
    listen       80;
    server_name  blog.chenyuhao.site;

    root   /usr/share/nginx/html;
    index  index.html index.htm;

    location / {
        # 解决单页应用服务端路由的问题
        proxy_set_header Host $host;
        try_files  $uri $uri/ /index.html;  
    }
}

